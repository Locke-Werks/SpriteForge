# Copy this file to `.env` and fill in the value. `.env` is gitignored and must never be committed.
# The key is read from the environment only — it never enters git, logs, or the game repo.
OPENAI_API_KEY=
